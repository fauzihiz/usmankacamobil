let currentSlide=0;let startX,isDragging=!1;function moveSlide(n){const slides=document.querySelectorAll('.carousel-slide img');const totalSlides=slides.length;currentSlide=(currentSlide+n+totalSlides)%totalSlides;updateSlidePosition()}
function updateSlidePosition(){const slides=document.querySelector('.carousel-slide');slides.style.transform=`translateX(-${currentSlide * 100}%)`}
document.addEventListener('DOMContentLoaded',()=>{updateSlidePosition();const carousel=document.querySelector('.carousel');carousel.addEventListener('touchstart',(e)=>{startX=e.touches[0].clientX;isDragging=!0});carousel.addEventListener('touchmove',(e)=>{if(!startX)return;let moveX=e.touches[0].clientX;let diffX=startX-moveX;if(diffX>50){moveSlide(1);startX=null;isDragging=!1}else if(diffX<-50){moveSlide(-1);startX=null;isDragging=!1}})})